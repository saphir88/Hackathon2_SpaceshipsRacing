{% extends 'base.html.twig' %}

{% block body %}
    <h1>Equipe edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <a class="btn btn-primary" href="{{ path('equipe_index') }}" role="button">Retour à la gestion des équipes</a>

{% endblock %}
